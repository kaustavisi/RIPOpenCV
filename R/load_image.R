
## mio <- Module("R2R", PACKAGE = "RIPOpenCV")
mi <- Module("R2R", PACKAGE = "RIPOpenCV")
mblur <- Module("Blur", PACKAGE = "RIPOpenCV")
mfeature <- Module("Feature", PACKAGE = "RIPOpenCV")
## mmorph <- Module("Morph", PACKAGE = "RIPOpenCV")
mgeo <- Module("geoTrans", PACKAGE = "RIPOpenCV")
mhist <- Module("hist", PACKAGE = "RIPOpenCV")

## box_blur <- function(inFileName, outFileName, blursize) {
##     mod <- Module("Blur", PACKAGE = "RIPOpenCV")
##     mod$boxBlur(inFileName, outFileName, blursize)
## }

## gaussian_blur <- function(inFileName, outFileName, blursize, sigmax=0, sigmay=0) {
##     mod <- Module("Blur", PACKAGE = "RIPOpenCV")
##     mod$gaussianBlur(inFileName, outFileName, blursize, sigmax, sigmay)
## }

## list2Array <- function(RImageList)
## {
    

## }
